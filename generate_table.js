const gh = require('./github');

const _ = require('underscore');

module.exports = {
	generateTable
};

// takes as input a list of labels - finds all the issues that have
// those labels and puts them into a table
function generateTable(labels) {
	
}
